# interfaces/voice/

Phase 3. STT/TTS, built as a thin wrapper over `interfaces/cli.ts` — not a
parallel system. No always-on wake-word / ambient listening (explicit
non-goal for v1).

Empty until Phase 3.
