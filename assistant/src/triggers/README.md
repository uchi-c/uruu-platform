# triggers/

Phase 3+. `schedule.ts` (cron-style proactive checks) and `events.ts`
(file-watch / webhook driven triggers). Every trigger fires through the same
permission gate as a manual command — proactive does not mean unsupervised.

Empty until Phase 3.
