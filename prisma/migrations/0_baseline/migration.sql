-- Baseline marker.
-- This project's existing tables were created via `prisma db push` before
-- migration history existed. This migration is intentionally a no-op and is
-- marked as applied via `prisma migrate resolve --applied 0_baseline` rather
-- than executed, so it never touches existing data.
SELECT 1;
